from constants import *

# ─── Solver (BFS) ─────────────────────────────────────────────────────────────
# Retourne la liste des directions à jouer pour résoudre le niveau,
# ou None si aucune solution n'est trouvée.
# Directions : constantes UP / DOWN / LEFT / RIGHT de constants.py

def solve(grid):
    # TODO : implémenter BFS
    # L'algorithme explore tous les états possibles (grille = état).
    # Chaque état est représenté par un tuple de tuples (hashable) pour
    # pouvoir être stocké dans un ensemble "déjà visités".
    # On retourne le chemin (liste de directions) menant à is_won() == True.
    return None
